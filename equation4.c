#include<stdio.h>
int main()
{
    float length, width, area;
     printf ("Enter any number:");
     scanf ("%f %f", &length, &width);
     area = (length * width);
     printf ("%f", area);
    return 0;
}