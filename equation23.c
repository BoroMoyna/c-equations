#include <stdio.h>
 int main()
 {
    float u, v, t, s;
        printf ("Enter any number:");
            scanf("%f %f %f", &u, &v, &t);
            s = ((u+v)*t)/2;
        printf ("%f", s);
    return 0;
 }