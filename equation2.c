#include<stdio.h>
    int main()
    {
        int a, b, c, sum;
        printf ("Enter any number:");
        scanf ("%d %d %d", &a, &b, &c);
        sum = a + b+ c;
        printf ("%d", sum);
        return 0;
    }