#include <stdio.h>
    int main()
    {
        float a, b, average;  
        printf ("Enter any number:");
            scanf ("%f %f", &a, &b);
               average = (a+b)/2;
        printf ("%f", average);
        return 0;
    }