#include <stdio.h>
    int main()
    {
        float a, b, c, average;
            printf ("Enter any number:");
                scanf ("%f %f %f", &a, &b, &c);
                    average = (a+b+c)/3;
            printf ("%f", average);        
        return 0;
    }